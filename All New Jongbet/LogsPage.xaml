<Page x:Class="All_New_Jongbet.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="LogsPage" Background="#f0fdf4">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Border Padding="40,25,40,15">
                <TextBlock Text="Logs" Foreground="#32325d" FontSize="28" FontWeight="Bold"/>
            </Border>
            <Rectangle Height="5" Fill="#5e72e4" RadiusX="2.5" RadiusY="2.5" Margin="40,0,40,25"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="40,0,40,30">
            <Grid.ColumnDefinitions>
                <!-- CHANGED: 너비 비율을 가변(*)과 고정(400)으로 조정 -->
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <!-- Left: System Logs -->
            <Border Grid.Column="0" Style="{StaticResource KPICard}" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="System Logs" Style="{StaticResource CardTitle}" FontSize="16" Foreground="#32325d"/>
                    <TextBox Grid.Row="1" x:Name="LogTextBox" FontFamily="Consolas" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap" AcceptsReturn="True" BorderThickness="0" Margin="0,10,0,0"/>
                </Grid>
            </Border>

            <!-- Right: Order History -->
            <Border Grid.Column="1" Style="{StaticResource KPICard}" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Order History" Style="{StaticResource CardTitle}" FontSize="16" Foreground="#32325d"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                        <ItemsControl ItemsSource="{Binding OrderLogList}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource OrderLogItemStyle}" BorderBrush="{Binding BorderBrushKey, Converter={StaticResource StyleConverter}}">
                                        <StackPanel>
                                            <TextBlock Text="{Binding TimestampDisplay}" HorizontalAlignment="Left" Foreground="Gray" FontSize="11" Margin="0,0,0,5"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding OrderTypeDisplay}" FontWeight="Bold" Foreground="{Binding BorderBrushKey, Converter={StaticResource StyleConverter}}" Margin="0,0,10,0"/>
                                                <TextBlock Grid.Column="1" Text="{Binding StockName}" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                                            </Grid>
                                            <TextBlock Margin="0,5,0,2"><Run Text="{Binding StockCode}" Foreground="Gray"/><Run Text=" | "/><Run Text="{Binding AccountNumber}"/></TextBlock>
                                            <TextBlock><Run Text="Price:" FontWeight="SemiBold"/><Run Text="{Binding OrderPrice, StringFormat=N0}"/><Run Text=" | "/><Run Text="Qty:" FontWeight="SemiBold"/><Run Text="{Binding OrderQuantity, StringFormat=N0}"/></TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
