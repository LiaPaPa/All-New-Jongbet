<UserControl x:Class="All_New_Jongbet.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Border Padding="40,25,40,15">
                <TextBlock Text="Logs" Foreground="{StaticResource PrimaryText}" FontSize="28" FontWeight="Bold"/>
            </Border>
            <Rectangle Height="3" Fill="{StaticResource AccentPurple}" RadiusX="1.5" RadiusY="1.5" Margin="40,0,40,25"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource KPICard}" Margin="40,0,40,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="System Logs" Style="{StaticResource CardTitle}" FontSize="16" Foreground="{StaticResource PrimaryText}"/>
                <TextBox Grid.Row="1" x:Name="LogTextBox" FontFamily="Consolas" IsReadOnly="True" 
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                         TextWrapping="Wrap" AcceptsReturn="True" 
                         BorderThickness="0" Margin="0,10,0,0"
                         Background="Transparent" Foreground="{StaticResource SecondaryText}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>