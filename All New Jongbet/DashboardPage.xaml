<Page x:Class="All_New_Jongbet.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="DashboardPage" Background="#f0fdf4">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Border Padding="40,25,40,15">
                <TextBlock Text="Dashboard" Foreground="#32325d" FontSize="28" FontWeight="Bold"/>
            </Border>
            <Rectangle Height="5" Fill="#5e72e4" RadiusX="2.5" RadiusY="2.5" Margin="40,0,40,25"/>
            <Grid Margin="30,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource KPICard}" Margin="10">
                    <StackPanel>
                        <TextBlock Text="TOTAL ASSETS" Style="{StaticResource CardTitle}"/>
                        <TextBlock Text="{Binding TotalAssetText}" Style="{StaticResource CardValue}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource KPICard}" Margin="10">
                    <StackPanel>
                        <TextBlock Text="TOTAL P/L" Style="{StaticResource CardTitle}"/>
                        <TextBlock Text="{Binding TotalProfitLossText}" Style="{StaticResource CardValue}" Foreground="{Binding ProfitLossColor}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource KPICard}" Margin="10">
                    <StackPanel>
                        <TextBlock Text="DAILY CHANGE" Style="{StaticResource CardTitle}"/>
                        <TextBlock Text="{Binding DailyChangeText}" Style="{StaticResource CardValue}" Foreground="{Binding DailyChangeColor}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="3" Style="{StaticResource KPICard}" Margin="10">
                    <StackPanel>
                        <TextBlock Text="DAILY CHANGE (%)" Style="{StaticResource CardTitle}"/>
                        <TextBlock Text="{Binding DailyChangeRateText}" Style="{StaticResource CardValue}" Foreground="{Binding DailyChangeColor}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,10,0,0">
            <StackPanel Margin="0,0,0,30">
                <Grid Margin="30,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource KPICard}" Margin="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <TextBlock Text="Asset Trend" Style="{StaticResource CardTitle}" FontSize="16" Foreground="#32325d" VerticalAlignment="Center"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <ToggleButton x:Name="Btn1m" Content="1m" Style="{StaticResource PeriodButtonStyle}" IsChecked="True" Click="PeriodButton_Click"/>
                                    <ToggleButton x:Name="Btn3m" Content="3m" Style="{StaticResource PeriodButtonStyle}" Click="PeriodButton_Click"/>
                                    <ToggleButton x:Name="Btn6m" Content="6m" Style="{StaticResource PeriodButtonStyle}" Click="PeriodButton_Click"/>
                                </StackPanel>
                            </Grid>

                            <lvc:CartesianChart Grid.Row="1" Series="{Binding SeriesCollection}" LegendLocation="None" Height="250" Margin="0,15,0,-10">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Labels="{Binding Labels}"/>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="Assets" LabelFormatter="{Binding YFormatter}">
                                        <lvc:Axis.Separator>
                                            <lvc:Separator Stroke="#f1f1f1" StrokeThickness="1"/>
                                        </lvc:Axis.Separator>
                                    </lvc:Axis>
                                    <lvc:Axis Title="Profit(%)" Position="RightTop" ShowLabels="True"/>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource KPICard}" Margin="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <TextBlock Text="Today's Orders" Style="{StaticResource CardTitle}" FontSize="16" Foreground="#32325d" VerticalAlignment="Center"/>
                                <Border Background="#e9ecef" CornerRadius="10" Padding="8,3" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding OrderQueList.Count}" FontWeight="Bold" Foreground="#525f7f"/>
                                </Border>
                            </Grid>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,15,0,0" Height="250">
                                <ItemsControl ItemsSource="{Binding OrderQueList}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="White" CornerRadius="4" Margin="0,0,5,8" Padding="12,8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0" Width="4" CornerRadius="2"
                                                            Background="{Binding CardBrushKey, Converter={StaticResource StyleConverter}}" Margin="-12,-8,8,-8"/>

                                                    <StackPanel Grid.Column="1">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="{Binding OrderTypeDisplay}" FontWeight="Bold" FontSize="13" Foreground="{Binding CardBrushKey, Converter={StaticResource StyleConverter}}"/>
                                                            <TextBlock Grid.Column="1" Text="{Binding OrderTime}" HorizontalAlignment="Center" FontSize="11" Foreground="Gray" VerticalAlignment="Center" FontFamily="Consolas"/>
                                                            <TextBlock Grid.Column="2" Text="{Binding OrderStatusDisplay}" HorizontalAlignment="Right" FontSize="12" Foreground="#525f7f" FontWeight="SemiBold"/>
                                                        </Grid>

                                                        <TextBlock Margin="0,4,0,0" FontWeight="SemiBold">
                                                            <Run Text="{Binding StockName}"/>
                                                            <Run Text="|" Foreground="LightGray"/>
                                                            <Run Text="{Binding StockCode}" Foreground="Gray" FontSize="11"/>
                                                        </TextBlock>
                                                        <TextBlock Margin="0,4,0,0" FontSize="11" Foreground="#8898aa">
                                                            <Run Text="계좌:"/>
                                                            <Run Text="{Binding AccountNumber}"/>
                                                        </TextBlock>
                                                        <TextBlock Margin="0,2,0,0">
                                                            <Run Text="{Binding OrderPrice, StringFormat=N0}" FontWeight="SemiBold"/>
                                                            <Run Text="원 /"/>
                                                            <Run Text="{Binding OrderQuantity, StringFormat=N0}" FontWeight="SemiBold"/>
                                                            <Run Text="주"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>

                <Border Style="{StaticResource KPICard}" Margin="40,10">
                    <StackPanel>
                        <TextBlock Text="Holdings" Style="{StaticResource CardTitle}" FontSize="16" Foreground="#32325d"/>
                        <DataGrid ItemsSource="{Binding AllHoldings}" MaxHeight="400">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Account" Binding="{Binding AccountNumber}" Width="*" CellStyle="{StaticResource CenterAlignedCellStyle}"/>
                                <DataGridTextColumn Header="Code" Binding="{Binding StockCode}" Width="Auto" CellStyle="{StaticResource CenterAlignedCellStyle}"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding StockName}" Width="1.5*"/>
                                <DataGridTextColumn Header="P/L" Binding="{Binding EvaluationProfitLoss, StringFormat=N0}" Width="*" CellStyle="{StaticResource RightAlignedCellStyle}">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{Binding EvaluationProfitLoss, Converter={StaticResource ProfitLossColorConv}}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Return (%)" Binding="{Binding ProfitRate, StringFormat={}{0:F2}}" Width="Auto" CellStyle="{StaticResource RightAlignedCellStyle}">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{Binding ProfitRate, Converter={StaticResource ProfitLossColorConv}}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Avg. Price" Binding="{Binding PurchasePrice, StringFormat=N0}" Width="*" CellStyle="{StaticResource RightAlignedCellStyle}"/>
                                <DataGridTextColumn Header="Price" Binding="{Binding CurrentPrice, StringFormat=N0}" Width="*" CellStyle="{StaticResource RightAlignedCellStyle}"/>
                                <!--<DataGridTextColumn Header="(PrevClose)" Binding="{Binding PreviousClosePrice, StringFormat=N0}" Width="*" CellStyle="{StaticResource RightAlignedCellStyle}"/>-->
                                <DataGridTextColumn Header="Change (%)" Binding="{Binding DailyFluctuationRate, StringFormat={}{0:F2}}" Width="Auto" CellStyle="{StaticResource RightAlignedCellStyle}">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{Binding DailyFluctuationRate, Converter={StaticResource ProfitLossColorConv}}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Tradable" Binding="{Binding TradableQuantity, StringFormat=N0}" Width="Auto" CellStyle="{StaticResource CenterAlignedCellStyle}"/>
                                <DataGridTextColumn Header="Qty" Binding="{Binding HoldingQuantity, StringFormat=N0}" Width="Auto" CellStyle="{StaticResource CenterAlignedCellStyle}"/>
                                <DataGridTextColumn Header="Evaluation" Binding="{Binding EvaluationAmount, StringFormat=N0}" Width="1.2*" CellStyle="{StaticResource RightAlignedCellStyle}"/>
                                <DataGridTextColumn Header="Amount" Binding="{Binding PurchaseAmount, StringFormat=N0}" Width="1.2*" CellStyle="{StaticResource RightAlignedCellStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>